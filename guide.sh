# TODO
need to change de database address from localhost to db(name that a gave to postgres instance) in settings.py.dev, inside casepro

# TODO
need to split config files in folders, like db folder, django folder, jenkis folder

# TODO
Change ports to expose in db config, like reference https://stackoverflow.com/questions/40801772/what-is-the-difference-between-docker-compose-ports-vs-expose

sudo docker-compose down

sudo docker-compose build --no-cache

sudo docker-compose up
